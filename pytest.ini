[pytest]
# 本机同时装了 PySide6(Qt6.10) 与 PyQt6(Qt6.11)。pytest-qt 默认会先加载 PySide6，
# 其旧版 libQt6Core 之后被项目代码的 PyQt6.QtWidgets 复用 → 缺 Qt_6.11 符号、整套用例
# collection error。项目本身只用 PyQt6，这里把 pytest-qt 的绑定钉到 pyqt6。
qt_api = pyqt6
