# 应用级静态数据

| 内容 | 位置 | 版本控制 |
|------|------|----------|
| 用户项目列表等 JSON | `user_projects.json` 等 | 小文件可提交；本地路径勿提交 |
| 缩略图 / 运行时缓存 | `cache/` | **忽略**（见 `.gitignore`） |
| 大型离线分类库 | （规划）外部包或下载脚本 | 不进主 repo |

标本 SQLite 在 **各项目** `<project>/_data/project.db`，不在此目录。
