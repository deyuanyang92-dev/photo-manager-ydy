# 为项目入口设计 — Claude 的设计记录

> 写给下一个 agent 和用户本人。记录**我（Claude）对这个软件的理解**、**项目入口的现状**、
> **我提出的设计**、**尚未拍板的问题**。
>
> 立场：这是设计稿，不是既成事实。文中「现状」段落是代码事实（带 file:line，已核对）；
> 「设计」段落是我的提案，未经用户确认前不得直接实现。
>
> 作者：Claude（Fable 5 / Opus 4.8） · 日期：2026-07-13 · 状态：**待用户确认**

---

## 0. 这份文档为什么存在

用户 2026-07-13 的原话（连续三轮纠正我）：

> "你还是没理解深刻，这只是一个照片保存设置，而是一个项目管理，工作区管理。因为有一些
> metadata 需要设置。**我确实很难和你沟通**。"

> "你到底懂不懂，你这个软件是干嘛的，虽然是照片管理软件，但更多应该是**室内拍摄 或物种鉴定
> 管理系统**了，，我们拍摄照片，整理照片，这些都是**跟着项目走**，然后可以根据照片整理
> **某个断面物种名录**等等。"

> "不对，**照片当然是产品**。"

> "如果开始一个新项目，**如何创建项目？？？**"

前一提交（`d4f8c82`）把顶栏面板做成了「照片保存设置」。**名字错了，定位也错了** —— 它把一个
项目/工作区管理入口降格成了存图路径选择器。这份文档先把定位钉死，再谈怎么改。

---

## 1. 这个软件是什么（定位，第一性）

**标本影像 + 物种鉴定的科研数据系统。** 不是相册，不是文件管理器。

两个**并列的产品**，谁也不服务谁：

| 产品 | 是什么 | 为什么是产品 |
|---|---|---|
| **照片（TIFF 母图）** | 景深合成后的无损母图 | 它本身就是成果：发表图版、图鉴用图、物种凭证影像。**这批母图是无价资产** —— 整个红线体系（TIFF 永不自动删 / cjxl 必须 `--distance 0` 位精确 / JPG 删除四道闸）存在的唯一理由就是保住它。 |
| **数据（鉴定与名录）** | 标本记录、学名、断面物种名录、Darwin Core、标签 | 照片没有数据 = 一堆认不出来的图；数据没有照片 = 没有凭证的表格。 |

**项目是把这两个产品拴在一起的骨架，metadata 是拴的绳。**

真实工作链条：

```
立项（航次/专项）
  └─ 项目 metadata：采集人、拍摄人、鉴定人、地区代码、年份、默认坐标、拍摄场地、编号规则
       └─ 断面 / 站位（B1、B2、日出海湾…）—— 采样单元，不是「文件夹」
            └─ 室内拍摄：激活编号(UID) → 拍 JPG → 景深合成 TIFF 母图 → 归档
                 │   UID 就是从项目 metadata 派生的：省-地点-站位-编号-存储-日期
                 │   （naming.py:42-60；缺 station 自动降一段，是对的）
                 └─ 鉴定：WoRMS 定种 → 分类卡片
                      └─ 产出：某断面物种名录 / 站位物种表 / Darwin Core / 标签 / 发表地图
```

**关键推论 —— 这是我之前反复没抓住的点：**

1. **「照片保存到哪」不是一个独立问题。** 照片天生属于某个标本、某个断面、某个项目。路径是
   项目结构的**副产品**，不是用户要做的决策。把它当面板主概念 = 本末倒置。
2. **metadata 不是「设置项」，是科学数据本身。** 采集人、地区代码这些字段会流进每一条标本
   记录、每一个 UID、每一张标签、每一份导出表。**资料不全 → 下游名录和导出就是废数据。**
3. **所以项目入口的真身是**：「我现在在为**哪个项目的哪个断面**工作」—— 它决定接下来拍的每
   张照片、写的每条记录归属谁、继承哪套 metadata。切换/新建只是入口的一部分；**项目资料必须
   和它在同一个概念里**。

---

## 2. 现状：现在到底怎么创建一个项目（代码事实）

用户问「如何创建项目？？？」—— 答案是**三步，而且资料是第三步、可选、入口藏在项目树里**。

### 第 1 步 · 建项目容器

入口有两个，都通向同一个对话框：
- 顶栏 📁▾ 面板 → 「＋ 项目」按钮（`app/widgets/workspace_breadcrumb.py:516`）
- 项目树 → 更多菜单 → 「新建项目文件夹…」（`app/views/project_tree_view.py:317`）

对话框 `NewSurveyProjectDialog`（`app/widgets/new_survey_project_dialog.py`）**只问 2 个字段**：

```
┌ 新建项目文件夹 ──────────────────────┐
│ 项目名称  [江苏盐城2026    ]  必填    │
│ 建在哪里  [/mnt/n/调查数据 ] [浏览…]  │
│ 将创建：  江苏盐城2026/               │
│                      [取消] [创建]    │
└───────────────────────────────────────┘
```
带路径预览 + 撞名拦截。确定后 `create_survey_project(parent, name=..., sites=[])`
（`project_tree_view.py:4955`）只建**一个空项目目录**——纯容器，**没有** `_data/project.db`，
不能拍照。

> **历史**：这个对话框原本问 6 个字段（项目名/位置/年份/采集人/地区代码 + 采样点多行列表），
> 一次建好「项目 + N 个采样点」。用户 2026-07-12 亲自砍到 2 个字段。旧代码全部按 §7 注释保留
> 在 `new_survey_project_dialog.py:29-40` 和 `project_tree_view.py:4930-4952`，服务层
> `create_survey_project(sites=[...])` 一直支持旧行为，**没改**。要恢复只需反注释。

### 第 2 步 · 建断面 / 采样点

建完自动跳项目树、焦点钉到新项目（`focus_project`，防止下一步把子目录建到别人家去——GUI 实测
抓到的 bug）。然后点「＋ 下级目录」逐个加 B1、B2……**任意层级，想套几层套几层**（R-006 修订）。
这些才是真正的拍摄工作区（首次进入才初始化 `_data/project.db`）。

### 第 3 步 · 填项目资料（← 问题在这）

项目树右键项目 → 「项目设置」→ `open_project_settings_dialog`
（`app/widgets/project_settings_dialog.py:80`），它把 `ProjectSettingsDrawer` 塞进对话框，
**6 个页签**（`project_settings_drawer_layout.py:73-78`）：

| 页签 | 管什么 | 存哪 |
|---|---|---|
| 概要 | 项目名/地区/年份/负责人 等 `projectMeta` | `project_settings` 表 |
| 保存 | `customStorages`（存储介质，R 前缀 = RNA） | 同上 |
| 人员 | `personnel`：采集人 / 拍摄人 / 鉴定人 | 同上 |
| 命名 | `codeLabels`：省/地点/站位 代码表 → **UID 直接由此派生** | 同上 |
| TIFF | `tiffFields`：写进母图的元数据字段 | 同上 |
| 打印 | 标签打印默认值（瓶签 / RNA 签） | 同上 |

**子工作区自动继承父项目的这些设置**（`project_settings_service.py:280-306`，nearest-wins，
空值不覆盖非空的继承值）—— 所以在项目根填一次，下面所有断面直接带出来。

### 现状的病

- 顶栏面板叫**「照片保存设置」**（`workspace_breadcrumb.py:440`），副标题写「照片保存在进入拍照
  的下级目录中」（:511）—— **整个面板的概念被降格成存图路径**。
- 建完项目**不引导填资料**，只弹一个提示框叫用户"自己去点项目设置"（`project_tree_view.py:4975`）。
- 顶栏面板里**没有任何资料入口** —— 资料只在项目树右键里，用户在拍照时想改改不了。
- 结果：**用户看到的就是一个存图路径选择器**。这就是他骂的东西。

---

## 3. 设计：项目入口应该是什么样

### 3.1 一句话

**顶栏 📁▾ 是「项目与工作区」入口 —— 我在为哪个项目的哪个断面工作，以及这个项目/断面的资料。**
不叫「照片保存设置」。

### 3.2 面板结构（改后）

```
┌ 项目与工作区 ─────────────────────────────────┐
│ 最近：[潮间带专项 › B1] [盐城2026 › 日出海湾] … │  ← 已有(d4f8c82)，保留
│                                                │
│ 项目    [江苏盐城2026            ▾]  ← 已有下拉  │
│ 当前目录 [B2                     ▾]  ← 已有下拉  │
│                                                │
│ ── 本项目资料 ────────────────────  ← 【新增】  │
│ 采集人 张三 · 地区代码 JSYC · 拍摄人 —          │  ← 摘要行，一眼看出缺什么
│ ⚠ 3 项未填（地区代码影响编号）  [ 项目资料… ]   │  ← 缺项警示 + 入口
│                                                │
│ [＋ 新建项目]  [＋ 新建断面/目录]   ← 已有，保留 │
└────────────────────────────────────────────────┘
```

新增的只有**一块**：本项目资料摘要 + 「项目资料…」按钮（复用已有的
`open_project_settings_dialog`，**不新写设置 UI**）。

### 3.3 新建项目流程（改后）

**不回到旧的 6 字段大表单**（用户 07-12 明确否掉了）。改成两段式：

```
[＋ 新建项目]
   ↓
① 建目录（现有 2 字段对话框，原样不动）
   项目名称 + 建在哪里 → 立即建出空项目目录
   ↓
② 紧接着弹「项目资料」（现有 ProjectSettingsDrawer 对话框，原样复用）
   顶部横幅："填了资料，下面所有断面自动继承，拍照时右栏直接带出来。
             也可以现在跳过，之后随时在顶栏改。"
   [跳过，稍后再填]  [保存]
   ↓
③ 落到项目树，焦点钉在新项目上，提示加断面
```

**关键：② 可跳过、不挡路，但它必须出现在流程里** —— 因为
- 建项目那一刻是用户脑子里资料最全的时候；
- 资料决定 UID、决定继承、决定导出，不能等到"想起来再去右键"；
- 跳过的话，面板的缺项警示（3.2）会一直提醒，直到填完。

**新建断面同理**：断面级只需问站位代码（`codeLabels.stations` 那一项），其余全继承。

### 3.4 为什么这样做，不那样做

| 方案 | 判断 |
|---|---|
| 回到 6 字段大表单一次问完 | ❌ 用户 07-12 已否。字段多 → 建个项目像填表 |
| 只在项目树右键放资料（现状） | ❌ 就是现在被骂的状态：入口太深，拍照时改不了 |
| 面板内嵌完整 6 页签设置 | ❌ 顶栏面板塞不下，也和项目树的入口重复两套 UI |
| **摘要 + 缺项警示 + 一键打开现有设置对话框（本案）** | ✅ 零新 UI，入口浅，缺什么一眼看见 |

### 3.5 要改的文件（预估）

| 文件 | 改动 |
|---|---|
| `app/widgets/workspace_breadcrumb.py` | 面板标题改名（:440）；副标题重写（:511）；新增资料摘要行 + 缺项计算 + 「项目资料…」按钮 → 发 `project_settings_requested` 信号 |
| `app/main_window.py` | 接新信号 → `open_project_settings_dialog(当前项目根)` |
| `app/views/project_tree_view.py` | `_new_region()` 建完项目后接着弹项目资料对话框（可跳过）；`_new_subfolder()` 断面级问站位代码 |
| `app/widgets/project_settings_dialog.py` | 加一个「首次填写」模式：顶部横幅 + [跳过，稍后再填] 按钮 |
| 新测试 | 面板显示资料摘要/缺项；跳过资料仍能建成项目；建完资料写进 `project_settings` 且子目录继承 |

**红线**：不碰 `create_survey_project` 的服务层签名（旧 `sites=[...]` 行为保留）；不删任何 §7
注释；「待导入 / 认领」相关入口一律不动（R-010 LOCKED）。

---

## 4. 尚未拍板（等用户回答）

1. **面板叫什么？** 我提「项目与工作区」。用户如果有更顺口的说法（比如干脆叫「项目」），按用户的。
2. **建项目后弹资料，是必填还是可跳过？** 我主张**可跳过 + 事后缺项警示**。如果用户认为
   「地区代码」这类影响 UID 的字段必须当场填，那就把它设为必填、其余可跳过。
3. **断面级新建要不要问站位代码？** 我主张问（因为它进 UID），其余全继承。
4. **资料摘要行显示哪几个字段？** 我暂定：采集人 / 拍摄人 / 地区代码（三个最影响下游的）。

---

## 5. 给下一个 agent 的话

- 读这份文档前，先读 `docs/USER_REQUESTS.md`（R-006 / R-009 / R-010 / R-011）和
  `docs/PROJECT_MEMORY.md`。用户不想把同一件事说第 N 遍。
- **不要**把这个面板再叫回「照片保存设置」。
- **不要**因为觉得「资料对话框多余」而把它从新建流程里精简掉 —— 它是这份设计的核心。
- 用户的场景原话要连同代码一起写进注释（R-011），不能只留在文档里。
