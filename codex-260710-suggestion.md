# 标本影像管理软件综合评估与优化建议

> 文档日期：2026-07-10  
> 评估者：Codex  
> 评估对象：`v0.56` 发布基线、当前 `main`，以及 2026-07-10 尚未提交的 Claude Code 工作区优化  
> 文档性质：评审与路线建议，不代表这些项目已经实施

## 0. 用户当前决策

本阶段只进行评估、提出建议和记录建议，**不实施代码重构，不修改软件功能**。

- 本文中的 P0–P3 项目全部是候选建议，不是已授权的开发任务；
- 后续由 Codex、Claude 和用户共同复审，确认优先级、范围与验收标准；
- 只有用户明确指定某个建议开始实施后，才进入代码修改；
- 实施前仍需重新检查 Git 状态，避开他人尚未提交的工作，并先建立定向回归保护；
- 未经用户确认，不批量拆分类、移动目录、修改数据库或改变界面。

## 1. 先说结论

这个软件不需要推倒重写。它已经形成了真实、复杂且有行业针对性的标本影像工作流，业务能力、数据安全意识和测试规模都明显超过普通原型软件。当前最准确的判断是：

- **业务成熟度高于架构成熟度**；
- **主要风险不是功能不够，而是修改一个功能时容易波及邻近功能**；
- 最优路线是以 `v0.56` 为可回退基线，进行小步、等价、可验证的整理；
- 不应把“代码更短”“目录更漂亮”当成成功，成功标准必须是用户行为、数据结果、照片文件结果和发布包行为保持一致；
- 任何人都不能承诺“绝对无 bug”。可以做到的是：明确风险、保留基线、增加回归测试、逐步降低缺陷概率，并提供可靠回退手段。

综合工程评价约为 **7/10**：可以继续稳定使用和迭代，但还不是可以低成本长期扩展的最优架构。

## 2. 本次评估的事实边界

### 2.1 当前规模

- `app/`：291 个 Python 文件，约 115,777 行；
- `tests/test_*.py`：179 个测试文件，约 60,811 行；
- 当前最大类：`ProjectTreeView`，约 4,355 行；
- 当前最大函数：`build_qss()`，约 1,280 行；
- 当前检测到 4 组模块循环依赖；
- `WorkbenchView` 直接依赖约 40 个内部模块；
- `ProjectTreeView` 直接依赖约 30 个内部模块；
- `app/services/` 中仍有 10 个文件直接依赖 PyQt6；
- 静态搜索得到约 721 处宽泛或裸异常捕获。该数字包含合理的关闭容错，不能直接视为 721 个缺陷。

### 2.2 版本与工作区状态

- `v0.56` 是已发布、可回退的版本；
- 当前 `main` 已经包含多项 `v0.56` 之后的稳健性修复，因此不能把当前源码直接等同于 `v0.56`；
- 工作区存在尚未提交的 Claude Code 优化，主要治理跨工作区数据库连接、只读汇总隐式迁移和 Qt 测试延迟销毁；
- 工作区同时存在诊断截图、本机网络脚本等未跟踪文件，目前不是严格意义上的干净发布工作区。

### 2.3 本次验证

本次执行并通过：

```text
python -m pytest \
  tests/test_project_summary_service.py \
  tests/test_project_tree_view.py \
  tests/unit/test_architecture_boundaries.py \
  tests/unit/services/test_service_domain_compatibility.py -q

73 passed in 10.86s
```

尚未在本次评估中重新验证：

- 全部 179 个测试文件；
- Windows EXE 重新构建与打包后烟雾测试；
- 真实相机、NIIMBOT 打印机、Helicon Focus；
- 两台真实电脑的局域网协作；
- 大型真实项目下的性能与长时间稳定性；
- 从多个历史版本数据库升级到当前版本的完整矩阵。

## 3. 当前设计中应当保留的优点

### 3.1 业务模块方向正确

现有“9 个业务模块 + 3 个基础模块”的地图合理：项目、调查、标本、照片工作台、影像资产、汇总报告、分类名录、标签打印、协作同步，以及应用外壳、持久化基础设施、维护交付。后续应落实这些边界，不必另起一套宏大架构。

### 3.2 数据和照片安全底线较清楚

- TIFF 原始成果不允许自动删除；
- JPG 只能在归档完整性检查成功后按规则清理；
- 项目数据库默认严格打开，不应因为路径不可用而创建“假项目”；
- SQLite 使用外键、忙等待、WAL/回退机制和备份接口；
- 最近新增的私有连接规约能够降低 Windows 跨工作区文件锁泄漏；
- 构建包已有用户数据、缓存和本机信息隔离意识。

### 3.3 已有较强测试资产

测试代码规模较大，已经有核心回归脚本、Qt 测试、服务测试、打包测试和兼容入口测试。真正需要做的是提高确定性、覆盖关键故障场景，并让测试结果更容易解释，而不是重新搭一套测试体系。

### 3.4 已经采用渐进式整理

部分服务已经迁入业务子域，同时保留旧导入路径兼容。这比一次性移动几十个文件安全。这个策略应继续使用。

## 4. 必须遵守的优化原则

1. **功能不变优先**：架构整理不改变入口、交互、字段语义、命名规则、数据库事实和照片结果。
2. **一个责任、一个提交、一个测试集**：一次只拆一个明确责任。
3. **先加保护再重构**：先用特征测试锁定当前行为，再移动代码。
4. **保持可回退**：保留 `v0.56` tag/Release；新版本使用新 tag，不覆盖旧包。
5. **界面与架构分开改**：不要在同一提交里同时美化界面和调整业务逻辑。
6. **数据库改动单独审查**：数据库 schema、迁移、删除、归档规则不得夹带在普通重构中。
7. **真实设备单独验证**：打印、相机、Helicon、局域网不能只靠 mock 宣布完成。
8. **不追求虚假指标**：文件行数下降不是目标；依赖方向、故障隔离和可验证性才是目标。

## 5. 优化建议总表

优先级定义：

- **P0**：下次面向更多用户发布前应解决；
- **P1**：近期逐步完成，直接降低回归和维护风险；
- **P2**：中期工程质量建设；
- **P3**：长期能力，当前不应抢占稳定性工作。

### 5.1 P0：发布、数据与回退底线

| ID | 建议 | 当前问题 | 建议动作 | 验收标准 |
|---|---|---|---|---|
| P0-01 | 冻结发布基线 | 已发布 `v0.56` 与当前 `main` 不同 | 保留 v0.56；后续优化进入新分支和新版本 | 能从 Release 重新下载并运行 v0.56；新版本不覆盖旧资产 |
| P0-02 | 建立干净发布门禁 | 工作区混有截图、本机网络脚本和修改文件 | 发布脚本拒绝脏工作区；按白名单收集源文件 | dirty tracked/untracked 状态下正式发布命令失败并说明原因 |
| P0-03 | 锁定 Windows 依赖 | 多数依赖只有下限，未来重建结果可能漂移 | 增加 Python 3.12/Windows 锁定或 constraints 文件；发布只用锁定依赖 | 同一提交在两台干净 Windows runner 得到相同依赖版本并通过测试 |
| P0-04 | 正式启用更新签名 | Ed25519 验证代码存在但公钥为空，当前签名层休眠 | 离线生成密钥；私钥放 Secret/离线备份；客户端内置公钥；Release 同时上传 `.sig` | 篡改 ZIP、缺少签名、错误签名均被拒绝；合法包可升级 |
| P0-05 | 规划 Windows 代码签名 | Windows 可能显示“未知发布者” | 评估 Authenticode 证书；与更新包 Ed25519 签名区分 | 发布说明明确两类签名用途；正式包可验证发布者 |
| P0-06 | 强化数据库迁移原子性 | 当前简单迁移可用，但复杂数据转换的事务和备份边界不足 | schema v3 前增加迁移前快照、逐迁移事务、失败回滚和幂等测试 | 中途故障后数据库可恢复；重复启动不重复破坏数据 |
| P0-07 | 建立历史数据库升级矩阵 | 只测当前 schema 不足以保护老项目 | 保存脱敏的 v0.54/v0.55/v0.56 示例库，自动升级并核对关键事实 | 每个历史样本均可升级；标本、名录、图片引用和采集记录数量一致 |
| P0-08 | 归档与删除故障注入测试 | JPG/TIFF/ZIP 规则是最高风险区 | 模拟磁盘满、权限拒绝、压缩中断、校验失败、进程退出 | TIFF 不被自动删除；未验证 ZIP 不触发 JPG 清理；失败可重试 |
| P0-09 | 发布前完整回归报告 | 局部通过容易被误报为全局完成 | 自动生成测试、构建、烟雾、未验证设备清单 | Release 附精确命令、通过数量、未测范围和残余风险 |

### 5.2 P1：架构与维护性

| ID | 建议 | 当前问题 | 建议动作 | 验收标准 |
|---|---|---|---|---|
| P1-01 | 拆分项目树 UI 构建 | `ProjectTreeView` 单类约 4,355 行，`_setup_ui()` 约 825 行 | 第一阶段只提取纯 UI builder、卡片和工具栏，不改查询/选择/导出 | 操作与截图关键结构不变；项目树定向测试全部通过 |
| P1-02 | 提取项目树查询门面 | 页面直接协调多个查询与数据库细节 | 建立 Qt-free 的只读 query service；统一跨工作区私有连接 | 页面不直接持有外部项目 DB；Windows 目录可在查询后移动/删除 |
| P1-03 | 收口工作台依赖 | `WorkbenchView` 扇出约 40 | 以 facade/dispatcher 暴露 monitor、compose、organize、result 四组控制器 | 主页面直接依赖目标逐步降到 20 以下；现有 signal/action 保持兼容 |
| P1-04 | 拆分协作服务 | `CollabService` 约 1,977 行，混合网络、状态、同步和 Qt 信号 | 分成 transport、peer trust、sync coordinator、store、Qt facade | 公共接口兼容；start/stop、断网、重复连接、退出测试通过 |
| P1-05 | 拆分主题生成器 | `build_qss()` 约 1,280 行 | 主题 token、控件片段、页面特例分离；输出保持快照兼容 | 同一主题生成的关键选择器和视觉快照无意外变化 |
| P1-06 | 消除 4 组循环依赖 | 主窗口/设置、工作台/弹窗、项目目录/索引、分组组件存在环 | 逐组通过接口、事件或共享 model 打断；一次只处理一组 | 静态依赖检测从 4 降到 0，且不使用延迟 import 掩盖结构问题 |
| P1-07 | 禁止新增 service → QWidget | 仍有打印执行器反向依赖 UI 等例外 | 把对话框适配移至 controller；架构测试禁止新增反向依赖 | service 层不导入页面/控件；必要 Qt 信号放明确 adapter/facade |
| P1-08 | 逐步减少 UI 直连数据库 | 页面仍有 `sqlite3` 或 DB manager 调用 | 将业务读写放入服务；UI 只接收 DTO/结果 | 新代码无 UI 直连 DB；旧调用按模块逐步清零 |
| P1-09 | 统一数据库连接所有权 | 缓存连接和私有连接过去容易混用 | 明确 GUI 主连接、worker 私有连接、跨工作区短连接三种契约 | 每种连接有创建者、关闭者和线程约束测试 |
| P1-10 | 建立后台任务所有权协议 | QThread、timer、executor 的关闭责任分散 | 统一 owned-task 接口：start/cancel/wait/dispose；页面关闭集中调用 | 连续打开/关闭页面和主窗口压力测试无孤儿线程、无崩溃 |
| P1-11 | 异常策略分级 | 约 721 处宽泛捕获，真实错误与可降级失败混杂 | 定义 fatal、recoverable、best-effort、shutdown 四类；按模块治理 | 文件/DB/网络错误有上下文日志；只允许指定边界静默失败 |
| P1-12 | 清理废弃但未接线代码 | 项目树有已标注的未接线内容模式 | 先搜索调用者并加行为证明，再删除或隔离 legacy 代码 | 无调用者证据、兼容测试和完整回归三者同时满足后才删除 |
| P1-13 | 继续按业务子域迁移服务 | 顶层 services 仍较扁平 | 修改哪个模块就迁移哪个叶子服务，保留旧路径 shim | 新模块进入正确子域；旧 import 仍指向同一对象 |
| P1-14 | 统一命令/结果对象 | 页面和服务间参数较多、字典形状分散 | 高风险流程使用 dataclass command/result，明确错误和取消状态 | compose/organize/sync 的输入输出可静态阅读并独立测试 |

### 5.3 P1：关键业务正确性

| ID | 建议 | 重点保护内容 | 验收标准 |
|---|---|---|---|
| P1-15 | 固化照片状态机 | incoming、selected、composed、organized、archived、deleted/kept 的转换 | 每个转换有前置条件、产物和失败回滚测试；禁止隐藏跳转 |
| P1-16 | 固化 UID 与手选 JPG 规则 | 手选 JPG 不应被“必须先激活编号”阻断；激活 UID 是默认上下文而非权限门 | 有 UID、无 UID、无选择、自动归档、外部 TIFF 场景均有回归测试 |
| P1-17 | 固化路径身份模型 | `/mnt/n/...` 与 `N:\...` 必须视为同一工作区 | recent project、owner path、UID 查询、打开文件均覆盖双路径形式 |
| P1-18 | 固化名录语义 | 名录是分类学物种清单，不是处理进度表 | 未鉴定材料不伪装为物种行；混合样本的额外鉴定可持久化和汇总 |
| P1-19 | 固化协作两套入口 | 团队永久码与项目共享码不是同一流程 | UI、状态机和测试分别覆盖加入团队与共享项目 |
| P1-20 | 明确审计日志边界 | 审计失败不能阻断编辑，但也不能完全无迹可查 | 编辑成功且审计失败时有本地诊断记录；连接必定关闭 |

### 5.4 P1：性能与资源

| ID | 建议 | 当前风险 | 验收标准 |
|---|---|---|---|
| P1-21 | 建立大项目性能基准 | 小型测试无法发现万级图片/标本退化 | 建立 1k、10k、50k 标本和图片的脱敏生成数据 | 项目打开、树选择、筛选、缩略图首屏有可追踪基线 |
| P1-22 | 查询计划与索引审计 | 跨项目汇总、筛选和树聚合可能产生重复扫描 | 对高频 SQL 保存 `EXPLAIN QUERY PLAN`；只按证据加索引 | 无无界全表重复扫描；索引写入成本有说明 |
| P1-23 | 缩略图任务去重与预算 | 大量图片可能造成重复解码、内存和线程拥塞 | 相同资产请求合并；限制并发、内存和缓存；失败哨兵可过期 | 快速滚动和切换项目不持续增长线程/内存 |
| P1-24 | 汇总结果增量失效 | 全量重复汇总可能阻塞 UI | 以数据库更新时间/资产索引版本决定缓存失效 | 修改后结果及时更新；无变化时不重复重算/写入 |
| P1-25 | 消除 UI 线程重操作 | 图片解码、文件扫描、网络和大查询不得阻塞 Qt 主线程 | 为关键操作增加耗时日志和主线程断言 | 常用点击在基准机器上保持响应，长任务可取消并显示真实进度 |

### 5.5 P2：测试、CI 与工程工具

| ID | 建议 | 建议动作 | 验收标准 |
|---|---|---|---|
| P2-01 | 增加覆盖率报告 | 先报告不设硬门槛，再对安全核心设分模块最低线 | CI 可查看行/分支覆盖；新增核心代码必须有测试 |
| P2-02 | 引入 Ruff | 初期只检查新增代码，逐步治理存量 | 新提交无新增未使用 import、明显错误和复杂度恶化 |
| P2-03 | 渐进类型检查 | 先覆盖 command/result、核心 service 和 DB adapter | 新公共接口有类型；类型错误不通过核心模块门禁 |
| P2-04 | 增加依赖安全扫描 | 使用 `pip-audit` 或等价工具，定期运行 | 高危漏洞有升级、隔离或风险接受记录 |
| P2-05 | CI 分层 | quick、core、full、packaging、hardware-manual 分层 | PR 快速得到反馈；Release 仍执行完整矩阵 |
| P2-06 | 保持测试进程隔离 | 网络、打印、Qt 原生状态容易污染 | 隔离 runner 继续保留；记录每个失败文件和退出码 |
| P2-07 | 管理 flaky 测试 | 不允许仅靠反复重跑掩盖问题 | flaky 有编号、负责人、复现条件和限期；稳定测试不自动重试 |
| P2-08 | 增加属性/故障测试 | 路径归一、命名序列、归档完整性适合生成式测试 | 随机路径和异常组合不破坏核心不变量 |
| P2-09 | 保存 UI 结构快照 | 只验证关键结构、可操作性和稳定 ID，不锁死像素 | 等价重构可进行；入口消失、控件禁用会被发现 |
| P2-10 | 构建产物 SBOM | 记录 Python 包、外部 CLI 和许可证 | 每个 Release 可追溯实际依赖版本和来源 |

### 5.6 P2：可观测性与支持

| ID | 建议 | 建议动作 | 验收标准 |
|---|---|---|---|
| P2-11 | 结构化日志 | 日志包含模块、操作、项目匿名 ID、耗时、错误链 | 用户报告“没反应”时能定位在哪一层失败 |
| P2-12 | 诊断包导出 | 一键导出版本、环境、脱敏日志、线程状态和配置摘要 | 不包含标本照片、密钥、完整本机路径和个人数据 |
| P2-13 | 统一用户错误提示 | 区分用户可操作错误、网络错误、数据损坏和程序错误 | 提示包含下一步动作；详细堆栈只进日志 |
| P2-14 | 长任务真实进度 | 进度来自 worker 已完成量，不用动画假装完成 | 取消、失败、部分成功和完成状态准确 |
| P2-15 | 性能遥测本地化 | 默认只在本地记录耗时，不上传用户数据 | 可比较版本性能；任何远程遥测必须明确征得同意 |

### 5.7 P2：安全与协作

| ID | 建议 | 当前风险 | 验收标准 |
|---|---|---|---|
| P2-16 | 明确局域网威胁模型 | 当前 HTTP + 配对/信任适合可信实验室网络，不适合敌对 Wi-Fi | 文档和 UI 明确适用范围；默认只绑定私有网络接口 |
| P2-17 | 强化设备身份 | 团队码不能单独作为长期身份凭据 | 逐步引入设备密钥、签名挑战或短期会话令牌 | 重放、伪造设备和被撤销设备无法继续同步 |
| P2-18 | 最小化协作权限 | 同步模块不应获得不必要的全项目文件访问 | 项目绑定、目录白名单和操作审计均有测试 |
| P2-19 | 密钥与敏感信息扫描 | 本机脚本、token、路径可能误提交 | pre-commit/CI 扫描密钥、私钥、token 和危险配置 | 发现敏感信息即阻止发布，并给出清理指引 |
| P2-20 | 远程适配器隔离 | LAN P2P 和远程中继应是两个适配器 | 远程服务故障不影响本地项目和 LAN 基本功能 |

### 5.8 P2：界面、易用性与可访问性

> 本节是后续建议，不要求现在立即美化，也不应与架构重构混在同一提交。

| ID | 建议 | 目标 |
|---|---|---|
| P2-21 | 统一页面密度层级 | 顶栏、页面标题、提示条、工具栏使用统一高度和间距，减少无意义空白 |
| P2-22 | 保留多主题/方案切换 | 原始方案永久保留；实验主题通过设置切换，不覆盖用户选择 |
| P2-23 | 统一主操作位置 | 刷新、导入、导出、筛选等同类动作在各页位置一致 |
| P2-24 | 清楚表达选择上下文 | 明确当前项目、工作区、UID、已选照片和操作影响范围 |
| P2-25 | 减少弹窗 | 页面空间足够时优先内联面板；危险确认和必须决策才使用对话框 |
| P2-26 | 键盘与焦点可用性 | Tab 顺序、快捷键、焦点环、Enter/Esc 行为可预测 |
| P2-27 | 色彩与字号可访问性 | 对比度、缩放、Windows 高 DPI、色盲情况下仍能区分状态 |
| P2-28 | 空状态与错误状态 | 空白页解释原因和下一步，不仅显示空表格 |
| P2-29 | 新手渐进引导 | 首次使用提示关键路径，但可关闭、可再次打开，不阻断专业用户 |
| P2-30 | 视觉回归基线 | 对关键页面保存可解释截图，人工确认后才更新基线 |

### 5.9 P2：文档与治理

| ID | 建议 | 目标 |
|---|---|---|
| P2-31 | 更新架构审计 | 旧审计数字已落后于当前 main | 每次大版本刷新规模、循环、扇出、边界例外和完成状态 |
| P2-32 | 增加 ADR | 记录数据库连接所有权、线程生命周期、协作信任模型和更新签名 | 后续代理不重复推翻关键决定 |
| P2-33 | 建立需求追踪矩阵 | 用户要求 → 规范 → 实现入口 → 测试 → 发布版本 | 可判断一项需求是否真的在可见产品中完成 |
| P2-34 | 分离历史计划和当前真相 | `docs/superpowers` 等历史计划不得当作当前完成证据 | 当前状态只由代码、测试、规范和运行截图共同证明 |
| P2-35 | 修复文档编码 | 部分中文文档在不同终端出现乱码 | 仓库统一 UTF-8；CI 检查 BOM/编码和异常替换字符 |
| P2-36 | 发布说明标准化 | 列出用户可见变化、数据迁移、已知问题、验证范围和回退方法 | 用户能判断是否升级以及如何恢复 |

### 5.10 P3：长期建议

这些事项只有在 P0/P1 稳定后才考虑：

- 为外部导入、导出、打印设备建立稳定插件接口；
- 将领域服务设计为可由 CLI 调用，便于批处理和灾难恢复；
- 增加只读项目检查器，用于不启动完整 GUI 的一致性诊断；
- 建立可脱敏的性能样本库和真实设备兼容矩阵；
- 如用户量扩大，再考虑崩溃报告服务、远程协作服务和企业部署策略；
- 暂不建议迁移到 Electron、Web 全栈或整体 MVVM 重写，收益不足以抵消回归风险。

## 6. 按模块复审建议

### M1 项目与工作区

- 统一项目身份、规范化路径和跨 Windows/WSL 表示；
- 项目目录索引与目录服务解除循环依赖；
- 所有跨项目只读访问使用短生命周期私有连接；
- 项目移动、重命名、离线磁盘、只读目录必须有故障测试。

### M2 调查与采集

- 调查、工作区、站位、断面、采集记录保持独立概念；
- 汇总查询不得反向修改子项目；
- 坐标导入、地图展示和地理编码故障要彼此隔离；
- 地图网络不可用时不阻塞本地数据录入。

### M3 标本与 UID

- 由唯一权威服务负责 UID 生成、解析和序列推进；
- 页面不得复制编号判断；
- UID 重命名必须原子更新图片归属、附加鉴定、打印队列和引用；
- 混合样本的 `additional_identifications` 在所有保存/迁移路径中保留。

### M4 照片工作台

- 先建立明确状态机，再拆 controller；
- 手动选择、激活 UID、自动归档是三个不同信号；
- worker 只执行服务给出的计划，不自行决定删除和归档规则；
- compose、organize、undo、外部 TIFF 导入都要验证失败恢复。

### M5 图像结果与资产

- 建立稳定 asset ID，不只依赖可变路径；
- 缩略图、原图、TIFF、ZIP 的缓存和生命周期分开；
- ZIP 完整性、源 JPG 消费状态和 TIFF 结果状态可审计；
- 任何清理动作先生成计划，确认后执行并记录结果。

### M6 查询、汇总与报告

- 坚持只读，不隐式迁移或修改被汇总项目；
- 数据来源、筛选条件和生成时间进入报告元数据；
- 大项目使用分页/流式输出，避免一次装入全部对象；
- Excel、HTML、CSV 同一事实口径由共享 DTO 产生。

### M7 分类学与物种名录

- 名录严格是 taxon checklist；
- 未鉴定材料进入处理汇总，不生成虚假物种；
- WoRMS 网络失败不能破坏本地分类数据；
- 分类名称变更保留原始名称、接受名、来源和时间。

### M8 标签与打印

- 排版数学保持 Qt-free；
- UI 对话框从 service 反向依赖中移出；
- 打印任务有预览、批次、重试、取消和审计；
- 测试默认使用虚拟打印机，真实硬件进入人工兼容矩阵。

### M9 协作与同步

- 团队加入和项目共享保持两套清晰流程；
- transport、trust、sync、store、Qt facade 分层；
- 冲突处理不覆盖本地数据，先备份再合并；
- 明确 LAN 安全边界和设备撤销机制。

### M10 应用外壳与配置

- `MainWindow` 只负责导航、布局和应用级生命周期；
- 设置 schema、默认值、迁移和 UI 表单分离；
- 页面注册使用稳定 ID，不依赖导航下标；
- 主题切换不重建业务对象或丢失页面状态。

### M11 持久化与运行基础设施

- 数据库连接和线程所有权形成 ADR；
- schema migration、backup、restore 有端到端测试；
- 所有临时文件使用唯一名称和原子替换；
- 缓存必须可删除、可重建，不成为事实来源。

### M12 维护、更新与交付

- 发布从干净 tag 构建，依赖锁定，产物签名；
- Release 包含哈希、签名、SBOM、测试摘要和已知问题；
- 更新失败保留旧版本和用户项目，不做半覆盖安装；
- 诊断包默认脱敏，绝不包含私钥、token 和用户照片。

## 7. 推荐实施顺序

### 阶段 A：确认 Claude Code 当前工作（1 个独立提交）

范围只包括当前未提交的跨工作区私有连接和 Qt deferred-delete 测试治理。

1. 审查 `open_project_db_private` 是否允许相关写入审计场景；
2. 运行项目汇总、项目树和数据筛选定向测试；
3. 运行数据库文件锁的 Windows 实际移动/删除验证；
4. 运行完整分文件测试；
5. 独立提交，不夹带截图和网络脚本。

### 阶段 B：完成 P0 发布底线

依赖锁定、迁移保护、更新签名、干净发布门禁、历史数据库升级矩阵。此阶段不拆大类。

### 阶段 C：第一次等价重构

只拆 `ProjectTreeView` 的 UI builder/widget。保留选择、查询、汇总、编号、名录和 TIFF 路径规则原样。

### 阶段 D：依赖治理

一次解决一组循环依赖；再处理 service → UI 反向依赖和 UI 直连数据库。

### 阶段 E：工作台与协作收口

分别进行，不在同一版本同时大改。先加生命周期和状态机测试，再拆 controller/facade。

### 阶段 F：质量与体验

逐步启用 Ruff、类型检查、覆盖率、诊断包、性能基准；最后再独立进行界面密度与视觉统一。

## 8. 每个优化提交的统一验收模板

```markdown
### 变更目的
- 为什么要改；不改会有什么风险。

### 行为不变量
- 哪些入口、字段、文件结果、数据库结果和提示必须不变。

### 修改范围
- 精确文件和责任；明确未修改的邻近模块。

### 风险面
- 可能影响的页面、服务、数据库、线程、设备和发布流程。

### 自动验证
- 精确命令、通过数量和失败数量。

### 人工验证
- 操作步骤、使用的数据、截图或日志证据。

### 未验证范围
- 真实硬件、网络、历史数据或打包环境中尚未覆盖的内容。

### 回退方法
- 对应基线、commit、tag 和数据恢复办法。
```

## 9. 建议 Claude 的复审方式

请 Claude 不要立即全面改代码，先逐条复审本文件，并对每条建议给出：

```text
ID：
结论：接受 / 部分接受 / 拒绝 / 已解决
证据：具体文件、函数、测试或运行结果
风险：实施后可能破坏什么
建议调整：范围、顺序或验收标准如何修改
是否涉及功能变化：是 / 否
建议版本：近期 / 中期 / 长期
```

Claude 复审时重点回答：

1. 当前未提交的私有数据库连接改动是否存在写入、事务或线程语义变化；
2. Qt deferred-delete 全局 fixture 是否可能掩盖产品代码中的真实生命周期缺陷；
3. 4 组循环依赖的最小打断点分别是什么；
4. `ProjectTreeView` 第一刀如何只拆 UI、不碰业务；
5. 更新签名启用后，旧客户端和无签名 Release 的升级兼容策略；
6. 哪些建议已有实现，需从“待办”改成“保持并加强验证”；
7. 哪些指标或判断缺少证据，需要补充脚本或实测。

## 10. 明确禁止的优化方式

- 禁止全量重写或换技术栈；
- 禁止一次性移动全部服务文件；
- 禁止用大量延迟 import 掩盖循环依赖；
- 禁止全库机械替换异常捕获；
- 禁止为了测试通过修改正确的业务语义；
- 禁止把线程崩溃简单归因于 pytest 而不检查产品生命周期；
- 禁止在架构提交中夹带 UI 改版、字段改名或数据库 schema 变化；
- 禁止对 TIFF/JPG/ZIP 删除规则做未经单独审查的调整；
- 禁止使用 `git add .` 发布包含本机脚本、截图、缓存或密钥的版本；
- 禁止用局部测试通过宣称整个软件“无 bug”或“全部完成”。

## 11. 最终建议

近期最有价值的工作不是继续增加功能，而是完成以下闭环：

1. 审核并单独提交 Claude Code 当前数据库连接与测试生命周期治理；
2. 建立发布可复现、更新可验签、数据库可回退三条底线；
3. 以项目树 UI builder 为第一项小步重构；
4. 每次重构都使用本文件第 8 节模板报告；
5. Codex 与 Claude 的意见发生冲突时，以可运行测试、真实数据、真实界面和用户确认作为最终证据，不以模型表述的自信程度作为证据。

这条路线速度可能不如一次性大改看起来快，但更适合当前软件：功能已经有价值，最重要的是在不破坏它的前提下，让后续每次修改更容易、更可预测、更安全。
