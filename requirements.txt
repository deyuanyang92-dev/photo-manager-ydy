# GUI 框架
PyQt6>=6.6.0
PyQt6-Qt6>=6.6.0
# PyQt6-WebEngine==6.10.*     # 已由 TileMapWidget 原生瓦片地图替代，不再需要
qtawesome>=1.3               # 矢量图标（Material Design Icons / Feather），替代 emoji

# 图像 / 二维码 / 导出
Pillow>=10.3.0
matplotlib>=3.8              # 采集地图出版底图渲染 + 导出（PNG/PDF/SVG/EPS）
pyproj>=3.6                  # 采集地图生成底图（Nature/R 风）投影变换；自带 PROJ wheel
qrcode[pil]>=7.4              # 标签二维码，纠错级 Q
python-barcode>=0.15          # 标签 1D 条码 (Code128)，可选：缺失时降级为占位框
openpyxl>=3.1                 # Excel 导出（34 列）

# 截图全局快捷键（可选：缺失时快捷键仅在窗口前台生效，不影响其它功能）
pynput>=1.7                   # 系统级全局热键（X11/Windows/macOS；Wayland 受限）

# 外部服务
httpx>=0.27                   # WoRMS 代理

# 协作（FastAPI 内嵌 + mDNS 自动发现）
fastapi>=0.110
uvicorn>=0.29
zeroconf>=0.131

# 测试
pytest>=8.0.0
pytest-qt>=4.4.0

# 说明：
#   sqlite3 = Python 内置，无需安装
#   cjxl / djxl = 系统级外部 CLI（Windows: 随包附带；Linux: apt libjxl-tools；macOS: brew jpeg-xl）
#   Helicon Focus = 不分发，只检测用户机器上的安装
