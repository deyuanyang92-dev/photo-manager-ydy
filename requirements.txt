# GUI 框架
PyQt6>=6.6.0
PyQt6-Qt6>=6.6.0
PyQt6-WebEngine==6.10.*       # 仅坐标交互地图视图用；必须与 PyQt6-Qt6 同版本，>= 会拉到 6.11 撞 ABI
qtawesome>=1.3               # 矢量图标（Material Design Icons / Feather），替代 emoji

# 图像 / 二维码 / 导出
Pillow>=10.3.0
qrcode[pil]>=7.4              # 标签二维码，纠错级 Q
openpyxl>=3.1                 # Excel 导出（34 列）

# 外部服务
httpx>=0.27                   # WoRMS 代理

# 协作（FastAPI 内嵌 + mDNS 自动发现）
fastapi>=0.110
uvicorn>=0.29
zeroconf>=0.131

# 测试
pytest>=8.0.0
pytest-qt>=4.4.0

# 说明：
#   sqlite3 = Python 内置，无需安装
#   cjxl / djxl = 系统级外部 CLI（Windows: 随包附带；Linux: apt libjxl-tools；macOS: brew jpeg-xl）
#   Helicon Focus = 不分发，只检测用户机器上的安装
