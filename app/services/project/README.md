# project — 项目 / 树 / 汇总

**职责：** 项目目录扫描、工作区识别、跨断面汇总、项目设置继承、汇总导出。

**已迁入：** `cover_pick_service.py`、`survey_overview_service.py`（扁平层保留兼容入口）。

**待迁入（扁平层）：** `project_service.py`, `project_tree_service.py`, `project_settings_service.py`, `cross_workspace_query_service.py`, `project_summary_service.py`, `project_adopt_service.py`, …

**迁移：** 见 `docs/migration-checklist.md` 阶段 2–3。
